Current display logo:

- `fragment-logo.png`

It was cropped from the provided horizontal PNG asset and converted to transparent RGBA.

If replacing with SVG, update `packages/brand/src/fragment.ts`:

```ts
logoPath: "/logos/fragment-logo.svg"
```
